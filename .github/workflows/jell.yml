name: Jekyll site CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@main
      with:
        repository: Samyakt24/Private1.git
        token: ${{ secrets.ACCESS_TOKEN }}
